import sqlite3

# db = sqlite3.connect('group_1081.db')  # Подключение к базе
# cursor = db.cursor()  # Вызвали метода что вносить ихзменения в Бд или получать
# cursor.execute('''
#     SELECT * FROM cinemas
# ''')  # execute() - Это метод в котором можем писать SQL запросы
#
# cinemas = cursor.fetchall()
# print(cinemas)
#
# new = 'Ормагедон'

# cursor.execute('''
#     INSERT INTO cinemas(cinema_name) VALUES (?)
# ''', (new,))
# db.commit()











